/**
 * Блик объектива. Эффект чисто экранный: он видит яркие пиксели кадра и рисует
 * для них артефакты, ничего не зная об источниках света — как блум. Позиция
 * звезды в расчёте не участвует и участвовать не должна.
 *
 * Свечением вокруг ярких пикселей владеет `BloomEffect` (`BLOOM_OPTIONS`);
 * здесь только артефакты объектива, пятью группами ручек:
 *
 * 1. общий множитель вклада эффекта в кадр — `intensity`;
 * 2. призраки, зеркальные отражения ярких пикселей через центр кадра —
 *    `ghostAmount`, `ghostThreshold`, `ghostAttenuation`;
 * 3. гало, кольцо вокруг центра кадра — `haloAmount`, `chromaticAberration`;
 * 4. лучи объектива (старберст), маска по крену камеры — `starburstAmount`;
 * 5. анаморфный штрих, горизонтальная полоса через яркий пиксель —
 *    `streakAmount`, `streakThreshold`, `streakScale`, `streakTint`.
 */
export interface LensFlareConfig {
  lensFlare: {
    /** Общий множитель артефактов в кадре */
    intensity: number
    /** Сила призраков — зеркальных отражений ярких пикселей через центр кадра */
    ghostAmount: number
    /** Сила гало — кольца вокруг центра кадра */
    haloAmount: number
    /** Разведение каналов в гало, в текселях половинного разрешения */
    chromaticAberration: number
    /** Сила лучей объектива; 0 — маска тождественна и лучей нет */
    starburstAmount: number
    /**
     * Порог, вычитаемый из каждой выборки призрака. Ожидаемый диапазон — от
     * нуля и выше; отрицательное значение не вычитает, а прибавляет.
     *
     * ЕДИНИЦЫ НЕ ЯРКОСТЬ ЭКРАНА. Вычитание идёт по значениям буфера, который
     * уже прошёл порог блума, даунсэмпл, предразмытие `KawaseBlurPass` с
     * `KernelSize.SMALL` и, последним перед этой выборкой, `LocalContrastMaterial`
     * (см. `LensFlareEffect.ts`) — то есть это уже РАЗНОСТИ с окрестностью
     * (пиксель минус среднее четырёх соседей на расстоянии
     * `LOCAL_CONTRAST_RADIUS`), а не яркости и не HDR-величины сами по себе.
     * Число не сопоставимо ни с `BLOOM_OPTIONS.luminanceThreshold`, ни с
     * яркостью пикселя на экране; осмысленный диапазон определяется только
     * замером, и смена `kernelSize` предразмытия ИЛИ `LOCAL_CONTRAST_RADIUS`
     * требует перезамера
     */
    ghostThreshold: number
    /**
     * Показатель затухания призраков к краю кадра: больше — плотнее к центру.
     * Должен быть строго положительным — при основании `pow` равном 0 (угол
     * кадра, где спад зажат в 1) показатель 0 или отрицательный даёт
     * неопределённое поведение в GLSL
     */
    ghostAttenuation: number
    /**
     * Сила анаморфного штриха; 0 — штриха нет.
     *
     * Значение выбрано ВЛАДЕЛЬЦЕМ на приёмке 04.08.2026: 0.03 он забраковал —
     * «слишком хорошо видна и выбивается на передний план», — и сила снижена
     * втрое. Это единственная ручка блика, чей текущий вид принят глазом, а не
     * выведен из арифметики; крутить её первой при любой жалобе на заметность
     * штриха.
     *
     * На той же приёмке штрих стал темнее ещё и сам по себе: до коммита
     * 6a70059 он прибавлялся ДО умножения на маску лучей объектива и получал от
     * неё множитель до 2.0 там, где маска яркая. Маска повёрнута по крену
     * камеры, так что по полосе ехали бы яркие и тусклые секторы, — штрих из-под
     * неё выведен. Значит числа этой ручки, замеренные до 6a70059, дают на
     * экране меньше, чем давали тогда, и напрямую с текущим не сравниваются.
     *
     * Почему значение упало с 5 до сотых (примерно в 167 раз). Причин ТРИ, и
     * ни одна сама по себе столько не даёт:
     *
     * 1. Число отсчётов 33 → 129. Сумма весов треугольного кернела равна
     *    `HALF_SAMPLES` (Σ(1 − |i|/N) по i от −N до N даёт N — это же написано
     *    в `AnamorphicStreakMaterial.ts`), то есть 16 → 64: ровно ЧЕТЫРЕ раза,
     *    а не «два порядка», как утверждалось здесь раньше.
     * 2. `streakThreshold` 0.9 → 0.3. Гейт множительный и не ограничен сверху:
     *    каждая выборка входит с весом `max(luminance − threshold, 0)`, так что
     *    снижение порога поднимает вклад всех выборок разом и тем сильнее, чем
     *    тусклее выборка.
     * 3. Смена источника на понижённый буфер (Kawase MEDIUM). Она работает в
     *    обе стороны: пики становятся ниже, но размытое пятно шире, и в гейт
     *    попадает больше отсчётов вдоль полосы.
     *
     * Разложение по этим трём причинам НЕ ЗАМЕРЕНО, и какая из них весит
     * больше — тоже: измеренным был только итог, форма полосы на TOI-519.
     * Точный вклад пунктов 2 и 3 требует отдельного замера, если он
     * когда-нибудь понадобится
     */
    streakAmount: number
    /**
     * Порог гейта штриха.
     *
     * ЕДИНИЦЫ НЕ ЯРКОСТЬ ЭКРАНА и не порог блума. Штрих читает собственный
     * источник — понижение предразмытого буфера ядром Kawase MEDIUM, — и меряет
     * значения именно этого буфера.
     *
     * НЕ ЗАМЕРЕН под текущий источник: 0.3 получено пробой, когда источником
     * был предразмытый буфер ПОЛОВИННОГО разрешения с ядром SMALL. Более
     * широкое ядро и понижение делают пики ниже, так что число стоит
     * перезамерить, если штрих полезет на тусклое или наоборот пропадёт.
     *
     * Понижение при этом вдвое сильнее, чем следует из размера таргета: сам
     * `streakSourceTarget` — четверть кадра, но у `KawaseBlurPass` есть свой
     * `resolutionScale = 0.5`, поэтому размытие идёт в одной ВОСЬМОЙ кадра, а в
     * четвертной таргет кладётся растянутая копия (см. `LensFlareEffect.ts`)
     */
    streakThreshold: number
    /**
     * Шаг отсчётов в текселях ИСТОЧНИКА штриха.
     *
     * Размер источника привязан к `resolutionScale` эффекта (публичная опция
     * `LensFlareEffectOptions`): таргеты штриха вдвое меньше половинных
     * таргетов эффекта, то есть их ширина равна `baseWidth * resolutionScale
     * * 0.5`. Значит один тексель источника — это `2 / resolutionScale`
     * экранных пикселей, а вылет полосы в экранных пикселях равен
     * `HALF_SAMPLES * streakScale * 2 / resolutionScale`.
     *
     * При штатном `resolutionScale = 0.5` множитель равен 4, то есть при
     * `HALF_SAMPLES = 64` и `streakScale = 5` — около 1280 пикселей в каждую
     * сторону: полоса пересекает кадр целиком, как в референсе владельца.
     * Крутить при жалобе «слишком короткий» или «слишком длинный»
     */
    streakScale: number
    /**
     * Оттенок штриха.
     *
     * НЕ перенос из three: у узла там почти чистый синий `[0.1, 0, 1]`, и на
     * широкой полосе он читается насыщенно-фиолетовым. Референс владельца —
     * холодная обесцвеченная полоса, отсюда подмешанные красный и зелёный
     */
    streakTint: readonly [number, number, number]
  }
}

/**
 * Значения подобраны на приёмке 02.08.2026 по замерам и по правке владельца:
 * блик обязан быть ненавязчивым.
 *
 * Самая тяжёлая сцена для блика — НЕ солнечная система, а чёрная дыра: там
 * ярче порога почти весь кадр, и артефакты зеркалят диск сам в себя. Именно
 * на ней и надо проверять всё, что касается пелены.
 *
 * ---
 * АРХИВ: замеры ПРЕЖНЕГО КОНВЕЙЕРА, 03.08.2026, сцена SAGITTARIUS A*.
 *
 * Читать как историю, а не как описание текущего поведения. Мерилось тогда,
 * когда призраки читали предразмытый буфер НАПРЯМУЮ — прохода локального
 * контраста (`LocalContrastMaterial`) ещё не существовало, и именно он потом
 * убрал ту самую пелену, по которой мерился потолок. Числа ниже про снятый
 * конвейер и к нынешнему отношения не имеют:
 *
 * при `intensity = 3` и `ghostThreshold = 0` тень заливалась молочной пеленой
 * с серо-фиолетовыми разводами; `ghostThreshold = 0.5` убирал разводы на той
 * же интенсивности; при `intensity = 8` они возвращались, а пара 0.5 с
 * `ghostAttenuation = 12` держала тень чистой до `intensity = 5`. Сверка
 * кадров солнечной системы в одной позе давала тогда среднюю разность 0.78 из
 * 255 при 2.6% изменённых пикселей.
 *
 * НИКАКОЙ действующей «границы», «потолка» и «запаса» из этих чисел не
 * следует: под текущим конвейером граница, на которой возвращается пелена, НЕ
 * ИЗМЕРЕНА ВОВСЕ. Замер — за владельцем, повторять на той же сцене
 * SAGITTARIUS A* и по одному изменению за раз.
 * ---
 *
 * `intensity` оставлена на 0.15: ненавязчивость — вкус владельца, а не
 * техническое ограничение, и поднимать её без его решения нельзя. Прежнее
 * значение было 0.01: при нём вклад эффекта не виден вовсе — подъём в
 * пятьдесят раз картинку почти не менял.
 *
 * Разбор 03.08.2026, сцена TOI-519, по одному изменению за раз. Общий корень
 * пелены во весь кадр и синих чёрточек на фоновых звёздах — отбор по
 * АБСОЛЮТНОЙ ЯРКОСТИ. Плато диска звезды ярче порога во всех своих пикселях,
 * и девять зеркальных копий этого плато кроют экран; фоновая звезда и главная
 * лежат в одном диапазоне HDR, так что порог, чистящий небо, гасит штрих и на
 * звезде. Заменено на отбор по локальному контрасту (призраки, гало) и по
 * размеру источника (штрих). Широкое коричневое свечение вокруг звезды при
 * этом остаётся: оно принадлежит блуму, а не блику — проверено обнулением
 * haloAmount.
 *
 * Цена. Локальный контраст — 5 выборок на пиксель половинного разрешения,
 * однократно (внутри выборок призраков это стоило бы около шестидесяти).
 * Штрих — 129 выборок на пиксель четверти разрешения: в кадре 2560x1261 это
 * примерно 26 млн обращений, вчетверо меньше, чем на половинном. Плюс проход
 * Kawase MEDIUM, готовящий источник штриха.
 *
 * Оба прохода штриха выполняются, только если `streakAmount` не ноль: при
 * нуле вклад штриха в кадр равен нулю по построению, и `LensFlareEffect`
 * пропускает их целиком. Обнулить ручку — законный способ вернуть кадру эту
 * цену.
 */
export const lensFlare: LensFlareConfig = {
  lensFlare: {
    intensity: 0.15,
    ghostAmount: 0.2,
    haloAmount: 0.04,
    chromaticAberration: 10,
    starburstAmount: 1.0,
    ghostThreshold: 0.5,
    ghostAttenuation: 12,
    streakAmount: 0.01,
    streakThreshold: 0.3,
    streakScale: 5,
    streakTint: [0.45, 0.6, 1.0]
  }
}
