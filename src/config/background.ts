/**
 * Конфиг фона сцены. Кубмапа восьмибитная, поэтому самая яркая звезда в ней
 * равна 1.0 и не может пробить порог блума (BLOOM_OPTIONS.luminanceThreshold).
 * Расширение хайлайтов подтягивает верх диапазона за 1.0, оставляя всё ниже
 * порога нетронутым.
 */
export interface BackgroundConfig {
  background: {
    /** Ниже этого значения фон не меняется вовсе */
    highlightThreshold: number
    /** Во сколько раз растягивается превышение порога; 1 — расширение выключено */
    highlightBoost: number
  }
}

/**
 * Значения подобраны на приёмке 02.08.2026 замером, а не на глаз.
 *
 * Порог 0.4, а не «почти единица», из-за минификации: грань 6000² приходит на
 * экран уменьшенной, ядро звезды усредняется с соседями ещё в фильтре, и до
 * расширения доживает уже не 1.0. Замер по грани `pz`: пикселей ярче 0.8 при
 * масштабе 1:1 — 6473, после уменьшения в 2.7 раза — 55. Поэтому порог 0.8
 * (первая проба) не менял картинку вовсе: сравнение кадров дало средний
 * модуль разности 0.09 из 255.
 *
 * Затронуто порогом 0.4 не больше ~0.04% пикселей неба — это ядра ярчайших
 * звёзд и самые светлые сгустки Млечного Пути; средняя яркость полосы при
 * включении выросла на 1.2%, то есть небо целиком не посветлело.
 */
export const background: BackgroundConfig = {
  background: {
    highlightThreshold: 0.4,
    highlightBoost: 9
  }
}
